<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.company</groupId>
        <artifactId>platform-parent</artifactId>
        <version>1.0.0</version>
        <relativePath>../../platform-parent/pom.xml</relativePath>
    </parent>

    <artifactId>my-redis-starter</artifactId>
    <name>my-redis-starter</name>
    <description>Redis 序列化、key 命名规范、Redisson、缓存模板（防穿透/击穿/雪崩）、分布式锁（@DistributedLock、LockTemplate）、滑动窗口限流（@RateLimit）</description>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson-spring-boot-starter</artifactId>
            <exclusions>
                <!-- Exclude broken Boot 3 auto-config; we provide our own Boot 4 compatible config -->
                <exclusion>
                    <groupId>org.redisson</groupId>
                    <artifactId>redisson-spring-data-35</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- Use Redisson core directly with our custom config -->
        <dependency>
            <groupId>org.redisson</groupId>
            <artifactId>redisson</artifactId>
            <version>3.37.0</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- 分布式锁/限流 AOP 支持：Spring Boot 4 已移除 spring-boot-starter-aop，直接引入 aspectjweaver -->
        <dependency>
            <groupId>org.aspectj</groupId>
            <artifactId>aspectjweaver</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>

        <!-- 可选：限流切面依赖 my-web-starter 提供的 BizException / CommonErrorCode；引入方为 Servlet 应用时生效 -->
        <dependency>
            <groupId>com.company</groupId>
            <artifactId>my-web-starter</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
